// Static topic counts per level, kept in sync manually with grammarData.js.
// Pages that only need counts (not full exercise content, e.g. StatsPage)
// import this instead of the ~650KB grammarData module.
export const grammarTopicCounts = { beginner: 22, intermediate: 8, advanced: 0 };
