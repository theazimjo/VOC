// Static topic counts per level/language, kept in sync manually with
// grammarData.js. Pages that only need counts (not full exercise content,
// e.g. StatsPage) import this instead of the ~650KB grammarData module.
export const grammarTopicCounts = {
  en: { beginner: 22, intermediate: 0, advanced: 0 },
  de: { beginner: 3, intermediate: 0, advanced: 0 }
};
